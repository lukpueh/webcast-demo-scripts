echo "should be running docker build with these args"
echo  $@
# our inspection can just use tar -xvf 
# we need to run in-toto verficiation here 

# replace with docker import using a hashed filename
